<UserControl x:Class="UI_ChausseeNeuve.Views.FileMenuView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" HorizontalAlignment="Center" Orientation="Vertical" Margin="0,20,0,0">
            <Button Content="Ouvrir" Style="{StaticResource PrimaryButton}" Click="Open_Click" Width="360" MinHeight="36" Margin="0,14,0,0"/>
            <Button Content="Ouvert récemment" Style="{StaticResource PrimaryButton}" Click="Recent_Click" Width="360" MinHeight="36" Margin="0,14,0,0"/>
            <Button Content="Enregistrer" Style="{StaticResource PrimaryButton}" Click="Save_Click" Width="360" MinHeight="36" Margin="0,14,0,0"/>
            <Button Content="Enregistrer sous" Style="{StaticResource PrimaryButton}" Click="SaveAs_Click" Width="360" MinHeight="36" Margin="0,14,0,0"/>
            <Button Content="Quitter le module" Style="{StaticResource PrimaryButton}" Click="Quit_Click" Width="360" MinHeight="36" Margin="0,14,0,0"/>
        </StackPanel>

        <Border Grid.Row="1" Margin="0,24,0,0" Padding="12" BorderBrush="#334444" BorderThickness="0" Background="#11FFFFFF" CornerRadius="8" Visibility="Collapsed" x:Name="RecentPanel">
            <StackPanel>
                <TextBlock Text="Récents" FontWeight="Bold" Margin="0,0,0,8" Foreground="White"/>
                <ItemsControl x:Name="RecentList">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Button Content="{Binding}" HorizontalAlignment="Left"
                                    Click="OpenRecent_Click" Tag="{Binding}" Style="{StaticResource PrimaryButton}" Width="360" MinHeight="36" Margin="0,14,0,0"/>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
