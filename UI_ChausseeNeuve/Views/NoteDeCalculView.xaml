<UserControl x:Class="UI_ChausseeNeuve.Views.NoteDeCalculView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <UserControl.Resources>
        <!-- Converter local -->
        <BooleanToVisibilityConverter x:Key="BoolToVisibility"/>
    </UserControl.Resources>
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="0,0,0,4" VerticalAlignment="Center">
            <TextBlock Text="Note de calcul" FontSize="20" FontWeight="Bold" Margin="0,0,16,0"/>
            <Button Content="Rafraichir" Command="{Binding RefreshCommand}" Margin="0,0,8,0"/>
            <Button Content="Exporter XPS" Command="{Binding ExportXpsCommand}" Margin="0,0,16,0"/>
            <CheckBox Content="Debug" IsChecked="{Binding ShowDebug}" Margin="0,0,8,0"/>
            <TextBlock Text="{Binding Status}" Margin="12,0,0,0" VerticalAlignment="Center"/>
        </StackPanel>

        <FlowDocumentScrollViewer Grid.Row="1" Document="{Binding Document}" Margin="0,12,0,12" />

        <ProgressBar Grid.Row="2" Height="6" Minimum="0" Maximum="100" IsIndeterminate="True" Visibility="{Binding IsBusy, Converter={StaticResource BoolToVisibility}}"/>
    </Grid>
</UserControl>